## 아키텍처 규칙

- 서버 컴포넌트에서 외부 API 를 부를 때는 `app/_libs/football/*` 의 함수만 사용한다.
  `footballServerFetch` 를 우회해 직접 `fetch` 하지 않는다.
- 클라이언트에서 외부 API 를 직접 부르지 않는다. 항상 `/api/football` 프록시를 경유한다.
  API 키가 번들에 포함되면 안 된다.
- 새 라우트 핸들러를 만들 때는 클라이언트 입력(쿼리 파라미터)을 화이트리스트/정규식으로
  검증한 뒤에만 외부 API URL 에 붙인다. `app/api/football/teams/[teamId]/matches/route.ts` 를 참고.
- ISR 주기는 `app/_constants/football.ts` 의 `REVALIDATE` 상수만 사용한다.
  숫자를 인라인으로 쓰지 않는다.
- 외부 API 실패는 화면 전체를 죽이지 않는다. 부분 실패는 빈 배열 폴백,
  페이지 단위 실패는 `ErrorNotice` 로 처리한다.
- 계산 로직은 `app/_libs/_utils/*` 에 순수 함수로 분리한다. 컴포넌트 안에서 계산하지 않는다.
- 타입은 `app/_types/*` 에 도메인별로 둔다. `any` 를 쓰지 않는다.
- 컴포넌트는 `_commons`(공용) / `_layouts`(레이아웃) / `_modals`(모달) /
  `_widgets/<도메인>`(도메인별) 로 분류한다. 각 컴포넌트는 자기 폴더에
  `<Name>.tsx` + `<Name>.module.scss` 쌍으로 둔다.
- 모달은 반드시 `useModalStore` + `ModalRoot` 를 통해 렌더한다. 개별 컴포넌트가
  모달을 직접 렌더하지 않는다.
- 로딩 스켈레톤은 그 화면의 **실제 레이아웃과 같은 골격**으로 만든다. 범용 막대나
  스피너로 때우지 않는다. 카드 테두리·그림자·열 너비를 실제 위젯과 맞춰 로딩 전후로
  화면이 밀리지 않게 한다.
- shimmer 는 `styles/skeleton.scss` 의 `skeleton-shimmer` / `skeleton-card` 믹스인만 쓴다.
  컴포넌트마다 `::after` 애니메이션을 복사하지 않는다.
- 데이터를 기다리는 라우트에는 `loading.tsx` 를 둔다. `loading.tsx` 는 `params` 를 받지
  못하므로, 탭처럼 하위 분기가 있는 화면은 공통 골격 + 기본 탭 모양으로 맞춘다.
- 주석은 "무엇"이 아니라 "왜"를 적는다. 코드로 드러나는 내용은 주석을 달지 않는다.

## 외부 API 사용 규칙 (무료 플랜)

- football-data.org 무료 플랜은 **분당 10회** 제한이다. 새 기능을 추가할 때는
  그 기능이 분당 몇 회를 소모하는지 먼저 계산하고, 예산을 넘으면 설계를 바꾼다.
- **N개 대회를 for 문으로 각각 호출하지 않는다.** `/v4/matches` 리스트 리소스에
  `date` / `dateFrom` / `dateTo` / `status` 필터를 걸어 한 번에 받고, 리그별 그룹핑은
  코드에서 수행한다.
- 무료 플랜은 스코어가 지연된다. UI 문구와 문서에 "실시간 / Live" 라는 표현을 쓰지 않는다.
  "자동 갱신" 으로 표기한다.
- 무료 플랜에서 **되는 것**: `/v4/teams/{id}` 의 스쿼드 명단(이름·포지션·생년월일·국적),
  `/v4/competitions/{code}/scorers` 의 선수별 득점·도움·PK·출전 경기수(`limit=100` 이면
  한 리그 전 팀을 덮는다), `season=YYYY` 로 과거 시즌 조회.
- 무료 플랜에 **없는 것**: 라인업, 교체, 카드, 등번호(팀 응답 기준), 득점 순위 밖 선수의
  개인 기록. 이 데이터를 전제로 한 기능을 제안하지 않는다.
- 가용성이 애매하면 문서를 믿지 말고 실제 엔드포인트를 한 번 호출해 확인한다.
- 새 기능을 붙인 뒤에는 `/budget` 화면으로 호출 수와 캐시 적중률을 확인한다.
  같은 경로의 "호출" 이 화면 수보다 크게 많으면 컴포넌트가 같은 리소스를 중복 요청하는 것이다.
- 시즌 같은 조회 조건은 페이지와 하위 컴포넌트가 **같은 값**을 써야 한다.
  값이 갈리면 캐시 엔트리가 두 벌로 나뉘고 화면에 다른 시즌 데이터가 섞인다.
- 429 응답은 정상 시나리오로 취급한다. 예외로 터뜨리지 말고 폴백 데이터를 반환한다.
- 시즌 개막 직후에는 순위·득점 기록이 비어 있다. 섹션을 통째로 숨기지 말고
  직전 시즌으로 폴백하고 어느 시즌인지 화면에 표기한다.

## 뉴스 (외부 RSS)

- 뉴스는 football-data 가 아니라 공개 RSS 에서 온다. `app/_libs/news/*` 만 사용한다.
- 제목·요약·링크·출처만 보여주고 본문은 원문으로 보낸다. 기사 전문을 복제하지 않는다.
- 피드 하나가 죽어도 나머지는 보여준다. 전부 실패했을 때만 `ErrorNotice` 를 띄운다.
- 피드가 미래 시각을 주는 경우가 있다(ESPN 은 서머타임 중에도 pubDate 를 EST 로 표기).
  `clampPublishedAt` 을 거치지 않고 시간을 그대로 표시하지 않는다.
- 목록은 순수 최신순으로 두지 않는다. 갱신이 잦은 한 소스가 첫 화면을 독점하므로
  `mergeNewsItems` 가 소스를 번갈아 배치한다.
